# Foundry build artifacts
cache/
out/
broadcast/
foundry.lock

# Static-analysis / fuzzer scratch (generated, not source)
crytic-export/
echidna-corpus/

# Editor / OS
.DS_Store
*.swp
*.swo

# Logs
*.log
data/extract.log

# Etherscan API response (contains the source we already extracted)
data/source.json

# Cloned upstream repos (not our code)
data/comrades_repo/

# Secrets — never commit
.env
.env.*
!.env.example
*.env
*.key
*.pem
secrets/
.secrets/

# Local-only viewer config
samples/viewer.local.*

# Foundry test dump artifacts (regenerated by `forge test`)
samples/*.html
samples/*.js
!samples/.gitkeep

# Vercel local config (project link, not source)
.vercel
site/.vercel/

# Python caches
__pycache__/
*.pyc
.gstack/
.vercel
